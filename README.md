# SEO OS (AEO + GEO + SEO)

## Run
```bash
pip install -r requirements.txt
python app.py
```
Then open: http://127.0.0.1:5000

## See it immediately
- Click **Instant Demo Report** (works without external network).
- If Live URL fails in your environment, use **Paste HTML** mode.
